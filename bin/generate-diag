#!/bin/bash
#
# Call diag generation script using sudo - since it
# requires root access to get at some system information.
#

archive_type=${1:-CallHome}
sudo /usr/libexec/pathologist/diagnostics/queue-diagnostic-archive $archive_type

# vim:autoindent
# vim:expandtab
